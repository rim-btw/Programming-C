# Лабораторная работа №2 (упрощённая): указатели и динамическая память

## Информация о студенте
**Фамилия, имя:** Бебутов Худайберди  
**Курс:** 1  
**Группа:** ИВТ-1

---

## Задача 3 – Максимум через указатели (task03.c)

### Постановка задачи
Цель: условие с обращением по указателю.  
Вход: два целых числа.  
Выход: максимальное из них.  
Требование: сравнивать значения через указатели.  
Пример: Ввод: 11 4 → Вывод: 11

### Математическая модель
max = a, если a > b, иначе max = b

### Список идентификаторов

| Имя переменной | Тип данных | Смысловое обозначение |
|----------------|------------|------------------------|
| a              | int        | первое число           |
| b              | int        | второе число           |
| pa             | int*       | указатель на a         |
| pb             | int*       | указатель на b         |
| max            | int        | максимальное значение  |

### Код программы (task03.c)

```c
#include <stdio.h>

int main() {
    int a, b;
    int *pa = &a, *pb = &b;
    scanf("%d %d", pa, pb);
    int max = (*pa > *pb) ? *pa : *pb;
    printf("%d\n", max);
    return 0;
}
