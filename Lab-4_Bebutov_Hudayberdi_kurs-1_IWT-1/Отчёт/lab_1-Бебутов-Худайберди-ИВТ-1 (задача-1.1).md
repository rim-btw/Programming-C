# Лабораторная работа №3: Структуры. Объединения. Перечисления.

## Информация о студенте
**Фамилия, имя:** Бебутов Худайберди 
**Курс:** 1  
**Группа:** ИВТ-1  

---

## Задача 1.1 – Структура с указателем на функцию

### Постановка задачи
Создать некоторую структуру с указателем на некоторую функцию в качестве поля. Вызвать эту функцию через имя переменной этой структуры и поле указателя на функцию.

### Математическая модель
Не требуется. Чисто программная конструкция.

### Список идентификаторов

| Имя переменной | Тип данных | Смысловое обозначение |
|----------------|------------|------------------------|
| func_ptr       | void (*)(void) | указатель на функцию |
| MyStruct       | struct      | структура с полем-указателем |
| s              | struct MyStruct | переменная структуры |

### Код программы  

```c
#include <stdio.h>

typedef void (*FunctionPointer)(void);

void myFunction(void) {
    printf("Функция вызвана через указатель из структуры!\n");
}

struct MyStruct {
    FunctionPointer func;
};

int main() {

    struct MyStruct s = { myFunction };

    s.func();

    return 0;
}
